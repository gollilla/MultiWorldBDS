# Runtime data (BDS world saves, downloaded binaries, logs)
data/

# Gradle
waterdog/plugin/.gradle/
waterdog/plugin/build/

# Node / CDK
node_modules/
infra/cdk.out/
infra/node_modules/
infra/dist/
dist*

# hakomc dev world save data
worlds/**/db
worlds/**/level.dat
worlds/**/level.dat_old
worlds/**/levelname.txt

.env

# Generated by docker-compose.yml's VARIABLES env var (itzg image), not
# hand-edited - see examples/worldControlCommands.ts
config/default/variables.json

# itzg unpacks a default permissions.json into docker/shared/config on first
# boot (behavior_packs doesn't have this problem - see the comment on
# DockerWorldProvisioner.sharedMountArgs for why only individual pack
# subdirectories are shared, never the parent) - not hand-authored, so don't
# track it.
docker/shared/config/default/permissions.json

# OS
.DS_Store
